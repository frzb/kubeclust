---
name: build
on: [push, workflow_dispatch]
jobs:
  integration_test:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Install Python 3 requirements
        run: pip install -r requirements.txt
      # There seems to be not Github Action for setting up Vagrant, so D.I.Y.
      - name: Download Vagrant
        run: curl -O https://releases.hashicorp.com/vagrant/2.2.9/vagrant_2.2.9_x86_64.deb -o vagrant.deb
      - name: Install Vagrant
        run: dpkg -i vagrant.deb
      - name: Spawn Vagrant VMs
        run: vagrant up
      - name: Make cluster
        run: make cluster
      - name: vagrant ssh -c kubectl get nodes
